# cached downloads + generated artifacts (the seeds in data/*.json ARE committed)
data/raw/*
!data/raw/.gitkeep

# secrets
.env

# python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# node (the Netlify function's only dependency)
node_modules/
package-lock.json

# netlify
.netlify/
