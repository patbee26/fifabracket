# Copy to `.env` and `source .env` (or export in your shell / CI secret).
# Optional: only needed to enable the football-data.org cross-check source.
# Free token: https://www.football-data.org/client/register
FOOTBALL_DATA_TOKEN=
